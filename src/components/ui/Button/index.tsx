import { withProperties } from '@src/utils/types';
import Submit from '@src/components/ui/Button/Submit';
import Button, { BlueRounded } from '@src/components/ui/Button/Button';

export default withProperties(Button, { BlueRounded, Submit: Submit });
