export interface LoginValues {
  email: string;
  password: string;
}

export interface LoginResponseValues {
  _id: string;
  email: string;
  phoneNumber: string;
  username: string;
  accessToken: string;
}
